# Copy to .env.local and fill in. Optional: without a key, the location
# input on "New scrape job" works as a plain text field (no autocomplete).
# Requires the "Places API (New)" enabled on the key's Google Cloud project.
VITE_GOOGLE_MAPS_API_KEY=
